```mermaid
sequenceDiagram
    participant UserAgent as User Agent (Browser)
    participant WebAPI as Web API (Server)

    Note right of UserAgent: Single request initiated by the User Agent

    UserAgent->>WebAPI: POST /exampleapp/new_note_spa
    activate WebAPI
    WebAPI-->>UserAgent: Status 201: Created
    deactivate WebAPI

    Note right of UserAgent: No redirect from server; User Agent remains on current page
    Note right of UserAgent: JavaScript processes response, updating SPA's notes list
```